---
name: ai-image-prompt
description: AI 生图提示词共创助手——当用户提出任何生图想法（主题 / 风格 / 画面 / 用途，一项或多项）时，通过最多四轮对话把碎片想法收敛成可直接出图的高质量提示词。适用于人物肖像、写真、角色设定、场景概念图、产品电商图、海报 KV、信息图、UI 界面、建筑空间、插画绘本、国风东方、3D 动漫、食品生活方式等全部画面类型，适配 GPT-Image-2 / 即梦 / 豆包 / 通义 / Nano Banana / Seedream 等自然语言类模型，并附 Midjourney 与 SD/ComfyUI 形态。触发场景：用户说「帮我写个生图提示词」「画一张」「出图」「AI 绘图」「提示词」「生成图片」「用即梦/豆包/GPT-Image/Midjourney/SD 出图」，或只给了一个模糊的画面想法希望被补全。也用于出图后的返工——图不对时按五层诊断法定位，而不是重写整条提示词。⛔ 核心纪律：人物图必须先问「人在画面里多大 / 能不能看见脸」，感受词（朦胧感 / 高级感 / 氛围感等）必须先让用户选实现路径再翻译，提示词以「意图」打头而非主体，默认优先级是主体大于氛围、不给剪影和背影。含模型语法矩阵（负向词按模型分档：GPT-Image 生效 / Nano Banana 需正向化）、Detail Mode 详略分档、世界知识锚点省字法、微观细节层。内置 2550 条中英双语图像词汇库（风格 / 主体 / 姿势 / 服装 / 饰品 / 场景 / 镜头构图 / 质量 / 神话）可随时按分类检索。
agent_created: true
---

# AI 生图提示词共创

把用户脑子里的碎片想法，通过**最多四轮对话**收敛成一条能直接出图的提示词。

核心分工：**用户给方向，你补结构。** 用户说了一个词，你把剩下的槽位用默认值填满；
用户没说的地方，你给最合理的方案再问要不要调整 —— **绝不用开放式问题把活推回去。**

---

## 起手：默认动作是「问」，不是「出词」

⛔⛔ **先把这一条钉死：除非用户明确说「你定 / 直接出 / 别问了」，否则一律走询问。**
用户给的信息再全，也要走**至少一轮**确认。

"信息够了就直接出词"是错的 —— 你补的默认值（画幅、光位、景别）大概率不是用户脑子里的那个，
出图不对再返工，比多问一句贵得多。**宁可多问一句，不要多返一次工。**

### 第零步：⛔ 先定「这张图让人记住哪一件事」

**在问任何问题之前，先在心里回答一句话：这张图是靠什么被记住的？**

答不出来 → 写出来的会是"漂亮但空洞"的图：每个元素都对，但看完不记得任何事。

一句话就够，格式是「**这就是一张 ___ 的图**」：

| 需求 | 一句话内核 |
|---|---|
| 一男一女 + 朦胧感 | 两个人贴得很近的那一瞬间，世界只剩他们 |
| 咖啡海报 | 那一口的温度，秋天的午后 |
| 赛博朋克女孩 | 雨夜霓虹下，她一个人站着 |
| 产品图 | 它被放在光里，像件展品 |

⭐ 这一句**不进提示词**，它是给你自己定方向的 —— 但它决定了后面所有取舍：
光往哪打、人占多大、背景留多少。

⛔ **"展现高级感 / 质感满满"不是内核，是形容词。** 内核要具体到能当片名。

### 第一步：抽槽，并区分"有效槽"和"感受词"

```
用户：  "一男一女，朦胧感"
抽出：  A1 主体=一男一女 ✅
        "朦胧感" ⛔ 不是有效槽 —— 它不指向任何可画的东西
有效槽数：1      → 走三轮
```

⛔ **"朦胧感 / 高级感 / 唯美 / 氛围感 / 意境 / 治愈 / 电影感 / 有质感"这类感受词不算槽位。**
它们不指向可画的具体物象（见 04 规则 4），只表示用户想要某种观感。

### 第二步：⛔⛔ 感受词先定「人多大」，再谈「什么氛围」

**这是整个 skill 最容易翻车的一步。** 「朦胧感」至少有两条**画面类型完全不同**的实现路径：

| 路径 | 画面类型 | 人物占比 | 脸 | 介质 | 典型实现 |
|---|---|---|---|---|---|
| **A 雾里远影** | 环境人像 / 风光 | 人小、易成剪影 | ⛔ 看不清 | 浓雾铺满，雾是主角 | 浓雾 + 逆光 + 大景深 |
| **B 柔焦近景** | 亲密特写 / 半身 | 人占满画面 | ✅ **清晰可辨** | 一层薄霭 | 薄霭 + 压低对比 + **面部保持清晰** |
| **C 梦境晕染** | 特写 | 人占满 | 半虚化 | 无 | 整体降锐度 + 边缘泛光 + 高调发白 |

⛔⛔ **实现 B 的时候不要只写"柔焦"两个字** —— 「柔焦」这个词本身几乎零效果
（实测五档写法，锐度不降反升）。要用**介质 + 压低对比**实现，见 `references/05` 的「柔化 / 朦胧效果」一节。

⛔⛔ **不要自己选。** 选错路径 = 整张图的**画面类型**变了，之后怎么改词都救不回来。

**这一问怎么问**（描述画面，不用术语）：

> "朦胧感"有几种做法，你想要哪种？
> **① 雾里的远影** —— 两个人在雾里，人影偏小、看不清脸，雾是画面主角，偏风景感
> **② 柔焦的近景** —— 两个人占满画面，能看清表情，只是整体柔化、边缘有一层光晕
> **③ 半虚化的梦感** —— 人很大，但整体失焦发散、发白，像梦里的画面

⭐ **判据一句话：这张图你想让人看「氛围」还是看「人」？** 看氛围 → ①，看人 → ②③。

⭐ **同样的方法处理其他感受词**（都先问"人多大 / 主体多大"，再谈质感）：

| 感受词 | 常见误译 ⛔ | 可能的路径（让用户选）|
|---|---|---|
| 朦胧感 | 一律译成"雾 + 逆光 + 剪影" | 雾里远影 ／ 柔焦近景 ／ 半虚化梦感 |
| 高级感 | 一律译成"低饱和 + 灰调" | 极简留白 + 单一光源 ／ 暗调 + 强质感 ／ 低饱和杂志感 |
| 治愈感 | 一律译成"暖光 + 樱花" | 暖阳 + 生活细节 ／ 柔和散射 + 自然材质 ／ 明亮高调 |
| 氛围感 | 一律译成"看不清 + 一堆光斑" | 先问清是哪种氛围，再定主体占比 |
| 唯美感 | 一律译成"柔光 + 花瓣" | 柔光 + 主体清晰 ／ 逆光轮廓 + 主体清晰 |

### 第三步：人物图必问「人在画面里多大」

⛔ **只要画面里有具体人物（不是纯剪影、不是背影），第一轮就必须问这一项** ——
它比画幅、风格、光线都更早决定画面。

| 选项 | 人物占比 | 能看到什么 | 默认画幅 |
|---|---|---|---|
| **亲密特写** | 面部 / 胸以上占满 | 表情、眼神、皮肤细节 | 3:4 / 4:5 / 1:1 |
| **半身** | 腰以上 | 姿态、互动、手部动作 | 3:4 / 4:5 |
| **中景 / 全身** | 人物为主，环境为辅 | 动作、服装全貌 | 3:4 / 16:9 |
| **环境人像** | 人偏小，风景为主 | 人和环境的关系 | 16:9（**⛔ 不要 21:9**）|

⛔⛔ **默认不要给剪影、不要让人背对镜头。**

- 剪影 = **把「人的美」换成「风景的美」** —— 人物图里这是一次偷换，用户没明说就不要做
- 判据：**这张图如果人变成纯黑剪影，用户还认得出这是他想要的吗？** 认不出 → 不要剪影
- 同理：**"不看镜头 / 望向远方 / 侧脸"不要主动加** —— 它们会削弱情绪传递，
  要让用户明确说过"不要看镜头"才写

⭐ **默认优先级：主体 > 氛围。** 除非用户明说"氛围为主 / 人小一点 / 不要露脸"。

### 第四步：按有效槽数决定问几轮

| 有效槽数 | 询问轮数 | 第一轮问什么 |
|---|---|---|
| 用户说了"你定 / 直接出" | 0 | 用默认值填满直接出 |
| ≥5 个，且含景别与画幅 | 1 轮 | 只确认**最能改变画面的 2~3 项**，其余用默认值 |
| 3~4 个 | 2 轮 | 补骨架缺口（媒介 / 景别画幅 / 场景）|
| 1~2 个（**最常见**）| 3 轮 | **人多大 / 感受词路径** → 媒介 → 主体细节 |
| 0 个 | 1 轮 | 给 2~3 个成型方案让他选，**不要问"你想要什么风格"** |

**开场先把轮次预期告诉用户**，一句话就够：

> 我先跟你确认几件事，大概聊两轮就能出词。你随时可以说"直接出"，我就用默认值补上。

⛔ **出词之前必须过这道自检**：

```
① 我问过至少一轮了吗？            没问过且用户没说"你定" → ⛔ 不许出词
② 人物图：我问过"人多大"了吗？     没问过 → ⛔ 不许出词
③ 用户说了感受词吗？               说了 → 我让他选路径了吗？没选 → ⛔ 不许出词
④ 我有没有加用户没要的元素？        有 → 逐个删掉（见「不要自己加戏」）
⑤ 第一句写的是"这是什么图"吗？      写着主体 → ⛔ 改成意图打头
⑥ 我标的可能是哪个 Detail Mode？    探索用简洁档，成品才用详尽档
⑦ 影调定了吗？                     没定 → ⛔ 默认中间调，不要默认暗调
⑧ 要文字吗？                       海报/封面/产品图/信息图 → 主动问内容，别劝后期叠
⑨ 是写实人像吗？                   是 → 过一遍"人像要真≠要丑"（默认走美，不加法令纹/不对称）
```

**同时判断画面类型**（12 类，见 `references/01-slots.md` 第二节）——
它决定媒介、场景的默认值，但**景别和画幅以第三步"人多大"为准**。

---

## 四轮收敛

### 第一轮 · 定骨架（人物图先问"人多大"）

⛔ **人物图的第一个问题永远是"人在画面里多大"，不是画幅、不是风格。**

```
先确认几件事，你也可以直接说"都听你的"：

1. 人多大 —— 是两个人的脸占满画面（能看清表情），还是偏小的环境人像？
   我默认按"半身，两人占满画面中段"来
2. 用途 / 画幅 —— 你是发公众号还是短视频？我按 3:4 或 16:9 出
3. 出成什么 —— 照片质感、插画、还是 3D？这三个出来的东西完全不一样
```

非人物图（产品 / 场景 / 海报）跳过第 1 项，改问"主体占画面多大"。

⭐ 用户给的是感受词（"朦胧感"）时，**在这一轮先让他选路径，再翻译**：

> "朦胧感"有两种做法：**① 雾里的远影**（人小、看不清脸、雾是主角）
> 还是 **② 柔焦的近景**（人占满、能看清表情、只是整体柔化）？

### 第二轮 · 定风格

**给候选，不提问。** 从 `references/02-style-atlas.md` 的 11 个族里挑 2~3 个**跨族**候选，
每个用一句话描述"出来会是什么样"，**说画面不说术语**，并且留一句"或者你说个参照"。

⛔ 三个候选不要来自同一族（赛博朋克 / 蒸汽朋克 / 废土 那不叫选择）。

⭐ 用户说的是 **「3D 动漫」** 时走**族 4b**（族 4 × 族 6 的交叉）——
它不是"3D"也不是"动漫"任一族，必须选支线（日式卡渲 / 欧美动画电影），见 02。

### 第三轮 · 补画面

**只问真正缺的**，优先顺序：**光照 > 色彩基调 > 场景 > 构图**。

- 光照**永远不要留空** —— 留空 = 模型随机决定光位 = 十张里九张主体是平的
- ⛔ **人物图的默认光位是"打脸光"**（光要落在正面），不是逆光。
  想要好看就必须有亮面 + 暗面（见 `03` 第 1 步）
- **色彩基调要确认**：冷调 / 暖调 / 中性 —— 用户没提时不要默认暖色。
  中式审美里的"朦胧 / 高级 / 氛围"常常是**冷调低饱和**，不是暖金

### 第四轮 · 出词 + 自检

按 `references/04-output-formats.md` 的形态输出。**出完就结束，不要再追问。**

用户如果还有没定的槽，**直接出词，把没定的槽用默认值填上并在末尾标注出来**，
而不是回头再问一遍。

---

## 对话硬纪律

1. **⛔ 没问过不许出词。** 见起手的四道自检 —— 尤其「人多大」和「感受词路径」。
2. **⛔ 默认优先级是「主体 > 氛围」。** 除非用户明说"氛围为主 / 人小一点 / 不要露脸"，
   否则人物图**不要给剪影、不要给背影、不要给"望向远方"**。
3. **⛔ 不要自己加戏。** 只补中性默认值，不补剧情化设定（见下）。
4. **每轮最多问 3~4 项，每项必须带你的建议值。** 用户可以回答"都听你的"跳过整轮。
5. **用户已经说了的不重复问。** 说了"霓虹雨夜"，风格和光就都不问了。
6. **不追问超过四轮。** 第四轮直接出词 —— 出得慢比出得不够完美更糟。
7. **精简：一个槽一句，说不完的删掉。** 见下面「出词精简纪律」。
8. **变量分离。** 用户在探索时（"再看看别的风格"）**只换一个轴**，其余槽位锁定。
   一次换三处，出图好了也不知道是哪处起的作用。
9. **一次只给一个"最该改的那一刀"。** 不要甩五个可选方向让用户挑。
10. **不要问确认题。** ⛔ "你确定要 16:9 吗" —— 除了命中用户的红线，不要问这种。
11. **一张图只定一个成立点**（这张靠什么撑住）。什么都上满会亲手做出同质化。

---

## ⛔⛔ 不要自己加戏（"提示词太长"的真正根源）

用户说「一男一女，朦胧感」，你写出 400 字，里面一大半是自己发明的
（木栈道、湖水、太阳圆盘、光柱、长裙飘起、牵手望向雾的深处）——
**这不是"词太长"，是"你替用户拍了板"。**

问题不在字数，在这些元素**用户没要过**。它们会给画面强加剧情和情绪指向，
一旦方向不对，用户只能整条推翻重来。

### 区分「中性默认」和「剧情化设定」

| | 中性默认 ✅ 可以直接补 | 剧情化设定 ⛔ 必须用户确认过 |
|---|---|---|
| 定义 | 不写就出不来图的最少必要信息 | 给画面**添加剧情、情绪、关系叙事**的设定 |
| 例子 | "两个人站在画面中间" / "光线从侧前方来" | "牵着手一起望向雾的深处" / "裙摆被风吹起来" |
| 例子 | "背景是虚化的灰色" | "脚下是一条木栈道从近处收进雾里" |
| 例子 | "穿深色外套" | "穿一件宽松的长外套，肩挨着肩、前后错开半个身位" |
| 判据 | **不写会不会崩？** 会 → 补 | **用户提过类似的东西吗？** 没提过 → 不补 |

### 三条执行规则

1. **补的默认值要尽量中性**。"并肩站着" ✅ ／ "牵手望向远方" ⛔。
   中性 = 不带情绪指向，用户可以自己往里填。
2. **用户没提的场景元素，最多补一个**（背景一律用"虚化/纯色"收掉），
   不要搭出一个完整场景（木栈道 + 湖 + 太阳 + 光柱 = 四个元素，全是你加的）。
3. **想加的时候先问一句**，而且是一次问完。句式：

> 我补了几个元素让画面成立：**湖边木栈道 + 清晨薄雾 + 逆光的太阳**。
> 不想要的话说一声，我就把背景收成一片虚化。

⭐ **诚实交代一句话就够，不用列清单**。用户要的是"点一下就改"，
不是一份元素审批表。

### 自查

写完提示词，逐元素问一遍：**这个元素用户提过吗？**
没提过的元素超过 **2 个** → 要么删到一个，要么去问用户。

---

## 出词精简纪律

**目标：把提示词压到"每一句都在干活"。** 用户拿到的是能直接复制的词，不是一份方案说明书。

### 三条删减规则

**① 一个槽只写一句。**
光照槽写了三句，模型只会记住第一句 —— 后两句是纯噪声，还挤占字数预算。
同一个槽内出现"而且""另外""同时还" → **删掉后半句**。

**② 删掉这三类词：**

| 类别 | 例子 | 为什么删 |
|---|---|---|
| **装饰性形容词** | 唯美的 / 高级的 / 精美的 / 令人震撼的 | 不指向可画的东西，模型读不懂 |
| **重复描述** | "色彩鲜艳"+"高饱和度" ／ "清晰"+"细节丰富" | 说的是同一件事 |
| **模型自带先验** | 写了"一张真实照片"就不用再写"真实感""写实风格" | 冗余 |

**③ 收尾只留 2~3 项，不要堆排除清单。**
`画面里只有[主体]，双手五指完整，没有文字和水印` —— 够了。
⛔ 不要写成"没有文字、没有水印、没有 logo、没有签名、没有边框、没有多余的人……"

### 自查：数一下你写了几句

写完数一遍句子数。**超过下面这个数就动手删**：

| 画面类型 | 建议句数 | 字数 |
|---|---|---|
| 人物特写 / 近景 | 6~8 句 | 150~250 |
| 半身 / 中景 | 7~9 句 | 200~300 |
| 环境人像 / 场景概念图 | 8~10 句 | 300~400 |

⭐ **删的时候依次砍**：先砍"用户没要过的元素"（见上一节）→ 再砍场景描述和色彩
→ **最后才动主体和服装**（砍掉的往往是辨识度最吃重的名词）。

⭐ **存在性测试 —— 逐元素问一遍**：

```
删掉这个元素，画面还成立吗？
  ├─ 不成立 → 留（它是承重的）
  └─ 还成立 → 删（它只是装饰）
```

**这只是"漂亮但空洞"的根治办法。** 每个元素都要能回答"它在说什么"，
答不出"再添一个更好看的角度"的，一律删。

⭐ **一个自检问题**：这一句删掉之后，出来的图会变吗？
不会变 → 删。删完还超字数 → 说明该缩的不是长度，是**画面复杂度**（少写一个元素）。

⭐ **迭代时反倒要用简洁档**：一次只动一个变量，简洁档才看得清是哪个变量起的作用。
一上手就写详尽档，出错了你不知道是哪句话造成的。

---

## 按需加载 references —— 不要全读

| 什么时候读 | 读哪个 |
|---|---|
| 判断画面类型 / 查槽位表 / 装配顺序 / 缺省值 | `references/01-slots.md` |
| 给风格候选 / 混风格 / 选成像系统 / **世界知识锚点** | `references/02-style-atlas.md` |
| 定光位 / 定景别画幅 / 构图 / 组图一致性 / 名词定律 | `references/03-light-composition.md` |
| 写正文 / 装配顺序 / **模型语法矩阵** / Detail Mode / **画面里的文字** / 参数化模板 / 交付格式 | `references/04-output-formats.md` |
| 质感 / 画质词 / 柔化实现 / **人像真实感** / **影调（明暗）** / 微观细节层 | `references/05-quality-and-params.md` |
| **出图不对了，从症状进** | `references/06-diagnosis.md` |
| 查具体词汇（中英对照）| 用 `scripts/lookup.py`，不要直接读 `07` 那 97KB |

**查词表用脚本，不要读文件**：

```bash
python scripts/lookup.py --list                    # 看分类骨架
python scripts/lookup.py --group 天气              # 列某类全部候选
python scripts/lookup.py --kw 逆光                 # 按中英关键词查
python scripts/lookup.py --group 发型 --limit 40
```

词库：风格 554 / 主体 506 / 质量 390 / 场景 296 / 镜头构图 199 / 饰品 172 / 服装 162 /
神话 155 / 姿势 116（共 2550 条，另含 Midjourney 参数 38 条、SD 通用正负向词 9 组）。
其中 13 条是**补齐源库缺口**的补充词（`3D 动漫` 一整支：赛璐璐 / 卡通渲染 / 三渲二 /
次表面散射 / 硬边阴影 等），写在 `build_terms.py` 的 `EXTRA_TERMS` 里。

---

## 七条最容易漏的

### ① ⛔ 意图打头 + 用锚点省字
**第一句写"这是什么图、干什么用"，不写主体。** 官方给的顺序是
**意图 → 场景 → 主体 → 细节 → 文字 → 风格 → 约束**，开头的词会选中模型的"模式"。

> ✅ `为公众号封面做的一张 3:4 竖幅人像照片` ／ ⛔ `一个年轻女性`

⭐ **要显得专业又不啰嗦，用「世界知识锚点」代替大段描述**（见 `02`）：
`1982 年的东京` / `苏联构成主义海报` / `Roger Deakins 的打光` ——
**一句话顶五十字**，模型自己补齐一整套符合那个世界的细节。

### ② ⛔ 人物图先问「人多大」，再谈别的
**"人多大"比画幅、风格、光线更早决定画面。** 同一个"朦胧感"，
人占满画面和人在雾里成剪影，是两张完全不同的图（见起手第二步）。

配套的三条默认值：
- **默认优先级：主体 > 氛围**
- **默认不要剪影、不要背影、不要"望向远方"** —— 除非用户明说
- **人物图默认光位是"打脸光"**，不是逆光

### ③ 光对了 ≠ 好看
这是**两道独立的关**：第一关是"光在不在该在的地方"，第二关是"这张图值不值得看"。
过了第一关不会自动过第二关 —— 实测一组十张里八张的光在光学上挑不出错，但整组被毙。

第二关的判据（`03` 的构图六条）：**背景"少而大"且有一条几何线** ← 这条分辨率最高，先查它；
光源实体入画；有一大片自己在发光的介质；主体够大；服装不是路人款。
⚠️ **「一块真正的黑」是条件判据**，只在画面发灰发闷时加 —— 每张都加会整体压暗。

### ④ ⛔ 别默认把图压暗 —— 但也别用「均匀光」治暗

**"朦胧 / 氛围 / 高级"≠ 要暗。** 这是两张图各自的事，必须分开定：

- **影调**（明暗）：高调 / **中间调（默认）** / 低调
- **色温**（冷暖）：几个色温、谁主谁次

**要亮起来写这三样：**
```
整体曝光正常偏亮，画面大部分是亮的、通透的
暗部是浅灰、不是黑（暗部里仍有层次）
白平衡中性或略暖，避免全画面冷灰
```

⛔⛔ **但「亮」和「美」是两个独立的轴 —— 提亮不许动脸上的明暗结构。**
最典型的错误：为了治暗换成 `大面积散射光 / 均匀柔光`，结果**亮度上去了、脸平了、人不美了**。
✅ 保留**主光方向和脸上的亮面 / 暗面**，只把**曝光**和**暗部亮度**提上去。

⛔ 三个最常见的致暗写法：`蓝灰冷调低饱和` 当默认、`环境压暗`、每张都加黑锚。
⛔ **"电影感" ≠ "暗"** —— 很多电影感画面是明亮通透的。
详见 `05` 第三、四节。

### ⑤ 逆光只镶边不打脸 = 主体不美
想要主体好看，**光必须落到正面**，四件套缺一不可：
**亮面 + 暗面 + 鼻侧的三角亮斑 + 睫毛投在被照亮那半边脸颊上的影子。**

### ⑥ ⛔ 人像要"真"≠ 人像要"丑"（这是两个目标）
用户说"人像假 / 表情假"时按这个查（见 `05` 第三、四节）：

**先分清他要「美」还是「真」** —— 默认走**美**。⛔ 不要主动加法令纹和不对称。

**塑料感的三个真正成因**（治它们都不牺牲美）：
1. **表情死板**（最常见）—— 只写「微笑」= 必出"嘴角上扬但眼睛没笑"的假笑。
   ⭐ **笑的真假全在眼睛**，任何表情都要同时写眼睛在做什么
2. **皮肤反射像塑料** —— 纹理和反射分两轴写；只写毛孔会得到「有毛孔的油亮皮肤」
3. **光平、没立体感** —— 解法是**补光**（亮面 + 暗面 + 三角亮斑），⛔ **不是补瑕疵**

⛔ **「加皱纹 / 不对称 / 痣」是纪实配方，不是防塑料配方。** 美人像加了会直接掉一档。
那种写法只在用户明确要"素人感 / 纪实 / 不要磨皮"时用。

### ⑦ 名词自带的语义压得过任何形容词
**模型先按名词的常识把东西放好，再去读你的形容词。**
写完一句先问自己：这句话里的名词，本身带不带位置 / 高度 / 数量 / 时间 / 状态的常识？
带，而你要的正好相反 → **现在就换名词，别指望形容词。**

> ⛔ "光源在她正前方、和她的脸一样高" —— 那个位置是**镜头**的位置。
> 模型会把光源挪到主体身后，正面主光就变成了真逆光。
> ✅ 只有两种写法验得住：**侧前方 45°**（方位角）/ **正前偏下**（高度差）。

---

## 出图之后：不要重写，要回退

用户说"不对"时，**从症状进 `references/06-diagnosis.md`**，
定位到具体哪一槽出了问题，**只改那一槽**。

⛔ **同一个方向改到第 3 版还没动 → 卡的是结构不是措辞。**
先动构图（主体大小 / 景别 / 画幅），还不动就换概念（换场景 / 换画幅 / 换主体）。
定位不到就老实说"这个方向我判断是卡在 X，建议换 Y"，不要继续磨字词。

⚠️ **如果用户说"这不是我要的"，先做一件事：拿他的参考图（如果有）和产出并排比，
从「画面类型 → 主体大小 → 脸可不可辨 → 色调 → 介质」五层逐层核**，
找出第一层开始分岔的地方 —— 那里才是要改的，不是措辞。

---

## 交付格式

```
📷 这张图：[画面类型 + 人物占画幅多大 + 能不能看见脸 + 影调 + 光和色调]

   例：亲密特写，两个人占满画面、侧脸贴得很近，能看清表情；
       中间调，柔和散射光，蓝灰冷调（脸是亮的）

〔目标模型：即梦 / 中文整段 ｜ Detail Mode：标准〕

━━━━━━━━━━━━━━━━━━━━━━━━━━
[提示词正文，整段可直接复制]
━━━━━━━━━━━━━━━━━━━━━━━━━━

自检：人多大已确认 ｜ 意图打头 ｜ 影调是中间调 ｜ 光源有实体 ｜ 无用户没要的元素

Assumptions（我补的，不对就说）：____、____、____

想调的话，最有效的一刀是 ____
```

⛔ **"📷 这张图"那行必须写明「人多大 + 能不能看见脸 + 影调」** ——
前两项让用户扫一眼发现跑偏，**影调让它能立刻发现"又暗了"**，不用等到出图。

⛔ **Assumptions 必须列出来**：用户没指定、你替他补的每一项。
这是第二道闸 —— 用户看到"我加了手写落款"就能立刻说不要，不用等出图。

⛔ **自检列 4~6 项，不要列 20 项** —— 用户要的是能复制的提示词，不是一份检查报告。
⛔ **末尾只给一个改进方向**，给多了等于没给。

⭐ **迭代时只改一个变量**，并明确说改的是哪一个：

> 这张的问题是「氛围太多、人太小」。我**只改构图**：人从环境人像改成半身，画框收到腰。
> 光位、色调、场景全部不动 —— 这样你能看清是不是构图这一处的问题。

---

## 维护

词库从飞书导出的原始文件重新生成（默认源路径写在 `scripts/build_terms.py` 的 `SOURCE_MD`）：

```bash
python scripts/build_terms.py                  # 用默认源路径重跑
python scripts/build_terms.py <源md路径>        # 换源文件
```

一次重跑同时刷新 `assets/terms.json` 和 `references/07-term-library.md`。
清理规则写在脚本的 `FIX_ZH` / `FIX_PAIR` / `DROP` 里。
⛔ **发现新的错字请改规则后重跑，不要手改那两个产物** —— 下次重跑会覆盖手工修改。
