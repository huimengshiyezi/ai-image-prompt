# 04 · 输出形态

**默认形态是"中文整段自然语言句"。** 用户点名 Midjourney / Stable Diffusion 时才切到附录里的形态。

---

## 主形态：中文整段句

适用于 GPT-Image-2 / 即梦 / 豆包 / 通义 / Nano Banana / Seedream 等一切吃自然语言的模型。

### 九条硬规则

1. **⛔ 意图打头 —— 第一句写"这是什么图、干什么用"，不是主体。**
   官方（OpenAI Cookbook）给出的顺序是
   **意图 → 场景 → 主体 → 关键细节 → 文字 → 风格 → 约束**。
   开头写 `为自媒体封面做的一张 3:4 竖幅人像照片` 或 `一张电影感的双人特写照片`，
   而不是直接 `一个年轻女性`。
   ⭐ 两个独立来源都说这是**提升最大的单点改动** —— 开头的词会选中模型的"模式"。
   中文自然语言类模型同理：先给"这是什么"，再给"里面有什么"。
2. **写句子，不写词组。** `一个年轻女性站在雨夜的巷口，霓虹灯从她右后方照过来`
   ⛔ 不是 `年轻女性, 雨夜, 巷口, 霓虹, 逆光`
3. **按 01 的装配顺序排**（意图 → 画幅景别 → 场景 → 主体 → 光色材质 → 文字 → 收尾）。
   **前面的句子会盖过后面的** —— 顺序错了信息就丢。
4. **一段一槽，用句号断开。** 不要写成一大坨逗号长句，模型会丢掉后半段的约束。
5. **每个槽都要落到具体物象。** ⛔ "氛围感 / 高级感 / 唯美 / 意境" 这类词零效果，
   它们不指向任何可画的东西，只会挤占字数预算。
6. **⛔ 一个槽只写一句。** 同槽内出现"而且""另外""同时还" → 删掉后半句。
   光照槽写三句，模型只会记住第一句，后两句是纯噪声。
7. **按 Detail Mode 决定详略，不要一律写满**（见下面一节）。
8. **负向词按模型分档**（见下面一节）—— ⛔ 不是"一律不用"。
9. **收尾固定一句**：`画面里只有[主体]，双手五指完整，没有文字和水印。`
   环境图去掉"浅景深"。⛔ 收尾句不要堆一长串排除项，只留这 2~3 项。

### Detail Mode —— 按用途决定详略

**详略跟着"这张图干什么用"走，不跟着画面类型走。**
同一个题材，做草稿探索和做最终成品，该写的字数差一倍。

| Mode | 用在哪 | 行数 / 句数 | 说明 |
|---|---|---|---|
| **简洁 Concise** | 试方向、批量探索、改一个变量再看 | 1~2 句 | 只写"意图 + 主体 + 关键光/景"，其余留白 |
| **标准 Standard** | 默认档，绝大多数情况 | 5~8 句 / 150~300 字 | 骨架填满，细节只给关键项 |
| **详尽 Verbose** | 要发出去的成品、复杂场景 | 8~12 句 / 300~450 字 | 加材质、微纹理、空气介质 |
| **电影级 Cinematic** | 主视觉、封面、campaign 中心图 | 12 句以上 | 在详尽之上再叠"微观细节层"（见 05）|

⭐ **探索阶段一律用简洁档** —— 一次只动一个变量，简洁档才看得清是哪个变量起的作用。
**不要一上手就写详尽档**：出错了你不知道是哪句话造成的。

⚠️ 长度仍然是**控景别的主变量**：写超了画框会自己扩大。
削长度时按顺序砍：**用户没要的元素 → 场景描述 → 色彩 → 最后才动主体和服装**。

### 负向词：按模型分档，不是一律不用

⛔ **"自然语言类模型都不吃负向词"是错的** —— 那是把 cfg=1 蒸馏模型的经验错套到了所有模型上。

| 模型 | 负向词 | 怎么写 |
|---|---|---|
| **GPT-Image-2 / 2.5** | ✅ **生效** | 可以显式写：`no watermark, no extra text, no logos, no extra fingers`。官方确认遵守 |
| **Nano Banana（Gemini）** | ⛔ 正向化 | 官方口径是"用正向描述"：✅ `clean background` ❌ `no clutter` |
| **cfg=1 蒸馏模型**（SD + Lightning/Turbo、各类加速模型）| ⛔ **填了不报错但不生效** | 只能正向化 |
| **即梦 / 豆包 / 通义** | ⚠️ **不稳** | 默认按正向化写；要排除时用「正向描述 + 末尾一句排除」双保险 |
| **Midjourney** | ✅ 生效 | `--no plants, text` |
| **SD（cfg>1）** | ✅ 生效 | 独立负向框 |

⭐ **拿不准就正向化** —— 正向化在任何模型上都不会错，只是啰嗦一点。
详见 `05` 的正向化公式。

### 模型语法矩阵（形态随模型变，不是一套打天下）

| 模型 | 提示词形态 | ⛔ 特别注意 |
|---|---|---|
| **GPT-Image-2 / 2.5** | 分段标签式：`用途 / 场景 / 主体 / 关键细节 / 文字 / 风格 / 约束` | **anti-slop**：`stunning / epic / masterpiece / 8K` 会**让结果变差**，不是无效而是有害 |
| **Nano Banana（NB2 / NB Pro）** | 自然语言段落，1~2 段 | ⛔ **不要写镜头数字**（`50mm` / `f/2.8` / `ISO 400`）—— 它直接忽略，要写 `浅景深`、`长焦压缩感` |
| **即梦 / 豆包 / 通义 / Seedream** | **中文整段自然语言**（本项目默认）| 中文写"描述性"而非"参数性"；镜头参数写感觉不写数字 |
| **Midjourney** | 英文逗号词组链 + 参数尾 | 见附录 A |
| **SD / ComfyUI** | 正向 + 负向 + 权重括号 | 见附录 B |

⭐ **镜头/相机参数的通用写法**：`50mm` 一类数字只有 GPT-Image 当"高层观感"接受，
其余模型要么忽略要么误读。**统一改写成语感描述**：
`浅景深` / `长焦压缩` / `广角的透视拉伸` / `贴近拍摄的近距离感`。

### 参数化模板 —— 一次出系列图时用

要出**同一套视觉、只换内容**的一组图（系列封面、角色多姿态、产品多角度）时，
**把变化项写成变量、其余锁死**。结构不变，模型得到的格式才一致。

```
场景：{地点，默认="蓝调时刻的里斯本小花店门口"}
主体：{人物，默认="穿藏青围裙的女性"}，{动作，默认="正在锁前门"}
关键细节：{光线}，{焦段感，默认="50mm 的观感"}，{主要材质}
用途：{用途，默认="杂志编辑类摄影"}
约束：{约束，默认="没有多余的招牌，背景里没有人"}
```

用法：**只改变量，不动结构**。一次改一个变量 —— 这同时也是"变量分离"的落地方式。

⭐ 简单版（三张以内）用不着模板，直接把变化项列出来给用户看：

> 这组我锁死：**蓝灰冷调 + 柔焦 + 半身构图 + 侧前窗光**。
> 只换三样：**场景**（书房 / 咖啡馆 / 雪原）、**服装**、**表情**。要不要这样出？

### 交付时标出 Assumptions

**用户没指定、你替他补的每一项，都要在交付里标出来。**
这是他一眼发现跑偏的第二道闸（第一道是"📷 这张图"那行）。

```
Assumptions（我补的，不对就说）：
· 画幅 3:4、半身 —— 你没说，按"亲密特写"默认
· 冷调蓝灰 —— 沿用你上次那组的调子
· 手写落款 —— 你没提，我按氛围图惯性加了，不要就删
```

### ⛔⛔ 只写「用户提过的 + 中性默认」，不写剧情

**这是"提示词太长"的真正根源** —— 不是形容词多，是你替用户拍了板。

| | 中性默认 ✅ 直接补 | 剧情化设定 ⛔ 要用户确认 |
|---|---|---|
| 定义 | 不写就出不来图的最少必要信息 | 给画面添加**剧情、情绪、关系叙事**的设定 |
| 人物 | "两个人站在画面中间，半身入镜" | "牵手一起望向雾的深处，肩挨着肩错开半个身位" |
| 服装 | "穿深色外套" | "宽松长外套 + 长及脚踝的连衣裙、裙摆被风吹起来" |
| 场景 | "背景是虚化的灰白色" | "脚下木栈道从近处收进雾里，两边湖水和天空连成两大块" |
| 判据 | **不写会不会崩？** 会 → 补 | **用户提过吗？** 没提过 → 不补 |

**自查：逐元素问"用户提过吗"。** 没提过的超过 **2 个** → 删到一个，或去问用户。

想补的时候**一次问完、一句话说完**：

> 我补了几个元素让画面成立：**湖边木栈道 + 清晨薄雾 + 逆光太阳**。
> 不想要就说一声，我把背景收成一片虚化。

### 画面里的文字：直接给内容 + 位置 + 形态

⛔⛔ **旧版这里写着"不要把文字内容写进提示词、建议后期叠字"—— 已经过时了。**
现在的模型（GPT-Image-2 / Seedream 4.5 / Nano Banana Pro / 即梦新版）**中英文字渲染已经很稳**。
**要字的图，就直接给字。**

⭐ **只要是海报 / 封面 / 产品图 / 信息图 / KV，默认就该问清文字内容，而不是劝用户放弃。**

### 三段式写法

```
文字：[确切内容]，[位置]，[字体形态 + 字号关系 + 颜色]
```

| 要素 | 怎么写 | ⛔ 不要 |
|---|---|---|
| **内容** | **用引号原样写出全部文字**，一个字都不改写 | 写个大意让模型自己发挥 |
| **位置** | `画面正上方` / `右下角` / `主体左侧三分之一处` —— 必须指定 | 不写位置（会压在主体上）|
| **层级** | 主标题大、副标题小，比例写清（`主标题字高约是副标题的两倍`）| 所有文字一样大 |
| **形态** | `粗黑体` / `纤细手写体` / `衬线宋体` / `等宽无衬线` | 只写"好看的字" |
| **颜色** | `深炭灰` / `米白` / 直接给色值 `#2C2C2A` | 不写颜色 |

### 示例

```
文字：画面正上方一行粗黑体主标题"秋日限定"，字高约占画面宽度的三分之一、深炭灰色；
主标题下方一行纤细无衬线副标题"2026 秋冬新品"，字高约为主标题的三分之一、同色；
两行文字居中，下方留出三分之一的空白放产品。
```

### 三条纪律

1. **要留白就同时写位置** —— `文字落在画面右三分之一，左侧留给主体`。
   ⛔ 只说"留白"不说在哪，模型不知道往哪留。
2. **文字和主体不要抢** —— 文字区域和主体区域要明确分开写。
3. **确认一下用户要的是"文字"还是"手写落款"** ——
   前者是设计元素（要清晰、要规范），后者是氛围配件（要随意、要轻）。

### 手写落款（氛围图 / 明信片 / 电影感海报）

这类是**氛围配件**，形态比内容重要：

| 要点 | 写法 |
|---|---|
| 形态 | `画面右上角有几行手写体中文小字，笔画细、力度轻，像随手写下的` |
| 位置 | 写明在哪一角（右上 / 左下 / 底部居中），⛔ 不写位置会飘到画面中间压住主体 |
| 留白 | 同时写 `留出周围的空白` —— 文字要落在空处，不能和主体抢 |
| 颜色 | 浅色字配暗背景，深色字配亮背景 |

⭐ 氛围图的手写落款**可以给内容也可以不给**：
给内容（`"冬与爱"`）能精确控制；不想定死内容就写 `几行看不清具体内容的手写小字`。
⚠️ 后者更容易被模型填成乱码，**要精确就写内容**。

### ⚠️ 什么时候仍然要提醒

只有一种情况：**文字量很大**（整段说明文字、信息图表里十几行字）。
这时候**分两批做**：先出图，再把文字用设计工具叠上去 ——
**不是因为模型写不好字，是因为排版精度不够**。少量文字直接写死就行。

### 删掉这三类词

| 类别 | 例子 | 为什么删 |
|---|---|---|
| **装饰性形容词** | 唯美的 / 高级的 / 精美的 / 令人震撼的 | 不指向可画的东西，模型读不懂 |
| **重复描述** | "色彩鲜艳"+"高饱和度" ／ "清晰"+"细节丰富" | 说的是同一件事 |
| **模型自带先验** | 写了"一张真实照片"又写"真实感""写实风格" | 冗余 |

### 标准段落模板

```
[画幅比例 + 景别]。                    ← 一张 3:4 的竖幅真实照片，从腰往上入镜。
[媒介 + 风格]。                        ← 电影感的写实摄影风格。
[主体 + 关键特征 + 动作 + 服装道具]。   ← 一个年轻女性……
[场景 + 时间 + 天气 + 背景结构]。       ← 她站在一条雨夜巷道里……
[光源 + 方向 + 落在哪 + 阴影]。         ← 正前方偏下一台自动贩卖机的冷白灯箱照上来……
[主色 + 第二个色温在哪]。              ← 整体青蓝冷调，唯一的暖色是右后方那块霓虹灯牌。
[介质 + 质感 + 细节]。                 ← 雨丝被灯光照成一条条亮线，地面把霓虹拉成长反光带。
[收尾]。                              ← 画面里只有她一个人，双手五指完整，没有文字水印。
```

### 精简对照：同一张图，长短两版

这两版**画面信息完全一致**，短版少了 100 字。删的都是不干活的字。

> **需求**：咖啡品牌秋日限定海报主视觉

**❌ 长版（292 字 / 8 句）**

```
一张 3:4 的竖幅商业产品照片，一杯拿铁居中，占画面高度的三分之二。

暖调的秋日静物摄影风格，真实照片质感。

一只白色陶瓷杯盛着拉花拿铁，杯壁有细微的釉面光泽，
杯子右后方斜倚着几支干枯的麦穗和两片枫叶。

木色桌面上，左侧是虚化的窗景，窗外是模糊的暖黄色秋树。

午后低角度的阳光从画面左后方斜射进来，穿过麦穗，在杯壁和桌面上
投下细长而清晰的麦穗影子，杯子的右半边落在柔和的暖褐色暗部里。

整体是暖褐与奶白的主调，窗外那一片冷灰的青蓝色天空是画面里唯一
的冷色，用来把暖调托起来。

空气里有细小的浮尘被阳光照成亮点，桌面木纹清晰可见，
杯口的奶泡有细腻的绵密质感。

画面里只有这只杯子和麦穗，没有文字和水印。
```

**✅ 短版（190 字 / 7 句）**

```
一张 3:4 的竖幅商业静物照片，一杯拿铁居中，占画面高度的三分之二。

一只白色陶瓷杯盛着拉花拿铁，杯壁有细微的釉面光泽，右后方斜倚着几支干枯的麦穗。

木色桌面上，左侧是虚化的窗景，窗外是模糊的暖黄色秋树。

午后低角度的阳光从左后方斜射进来，穿过麦穗，在桌面投下细长的影子，
杯子的右半边落在暖褐色暗部里。

整体暖褐与奶白，窗外那片冷灰青蓝的天空是唯一的冷色。

空气里有浮尘被照成亮点，杯口的奶泡绵密细腻。

画面里只有杯子和麦穗，没有文字和水印。
```

**长版删掉了什么：**

| 删掉 | 属于哪一类 |
|---|---|
| 「暖调的秋日静物摄影风格」| 模型自带先验 —— "商业静物照片"已经说了 |
| 「真实照片质感」| 重复描述 —— "照片"已说 |
| 「两片枫叶」| 多余元素 —— 一个道具够了 |
| 「在杯壁和桌面上」→「在桌面」| 简化，信息量不变 |
| 「用来把暖调托起来」| 解释性文字 —— 模型不需要你的设计意图 |
| 「桌面木纹清晰可见」| 与"浮尘被照成亮点"争夺注意力，删掉画面更干净 |

---

## 什么时候输出英文

| 情况 | 输出 |
|---|---|
| 默认 | 中文整段 |
| 用户点名 Midjourney / SD | 英文词组链（见附录 A / B）|
| 用户在做英文平台内容 / 主动要英文 | 英文整段自然语言 |
| 用户要双语对照 | 先中文整段，再给英文整段，**两段语义必须一一对应** |

⛔ **双语版不要逐字直译** —— 中英的摄影术语不对称。
"逆光"对应 `backlight`，"发丝光"要写 `rim light on hair strands`，
直译成 `hair silk light` 模型读不懂。术语对照查 `scripts/lookup.py --kw <词>`。

---

## 交付格式（给用户看的样子）

输出提示词时，**提示词本身要单独成块、可直接复制**，前面附一行"这张图长什么样"的说明，
后面附自检结果。不要只甩一段文字。

```
📷 这张图：9:16 竖幅，电影感写实，雨夜霓虹下的半身人像，青蓝冷调 + 暖橘补色

━━━━━━━━━━━━━━━━━━━━━━━━━━
[提示词正文，整段可复制]
━━━━━━━━━━━━━━━━━━━━━━━━━━

自检：画幅与景别匹配 ✅ ｜ 光源有实体入画 ✅ ｜ 两个色温 ✅ ｜ 有一块黑锚 ✅
      背景为块面结构 ✅ ｜ 无负向词 ✅

想调的话，最有效的一刀是 ____（写出最该改的那一个槽）。
```

⭐ **自检只列 4~6 项，不要列 20 项。** 用户要的是能复制的提示词，不是一份检查报告。
**末尾必须给出"最该改的那一刀"** —— 一次只给一个方向，不要甩五个选项。

---

## 附录 A · Midjourney 形态

**形态**：英文逗号词组链 + 参数尾。

```
a young woman standing in a rainy cyberpunk alley, neon signs, wet asphalt,
cyan and blue color grading, cinematic lighting, backlit rim on hair,
shot on 85mm lens, shallow depth of field --ar 9:16 --stylize 250 --v 6.1
```

| 要点 | 说明 |
|---|---|
| 语序 | 权重从高到低，最前面的词影响最大 |
| 词组 | 每个词组 2~4 个词，逗号分隔，**不写完整句子**（除了场景句）|
| 权重 | `word::2` 提权，`word::0.5` 降权；⛔ 不要滥用，超过 2 个权重就难控 |
| 负向 | `--no plants, text` 在 MJ 里**是真生效的**（和自然语言类不同）|
| 常用参数 | `--ar 16:9` 比例 ｜ `--stylize 0-1000` 风格化强度 ｜ `--chaos 0-100` 多样性 ｜ `--seed N` 复现 ｜ `--sref <图>` 风格参考 ｜ `--cref <图>` 角色参考 ｜ `--cw 10-100` 角色参考范围 |

⭐ **角色一致**：`--cref <角色图链接> --cw 10` 起手。
`--cw` 默认 100 偏大、一致性反而差，**从 10 开始试**。

⭐ **一组图要一致**：固定 `--sref` + `--seed`，只改场景和动作句；
但要拉开的话按 03 的六个轴来，别只换穿搭。

## 附录 B · Stable Diffusion / ComfyUI 形态

**形态**：正向 + 负向 + 权重括号。

```
正向：
1girl, short black hair, wet hair, dark reflective hooded jacket,
narrow rainy alley, neon signs, vending machine, (cinematic lighting:1.2),
cyan and teal color grading, orange neon rim light, 85mm, depth of field,
masterpiece, best quality, highly detailed

负向：
(worst quality:2), (low quality:2), lowres, bad anatomy, bad hands,
missing fingers, extra digits, watermark, signature, text, jpeg artifacts
```

| 要点 | 说明 |
|---|---|
| 权重 | `(word:1.2)` 提权，`[word]` 降权；**1.1~1.4 之间就够**，堆到 2 会畸变 |
| 正负向 | SD 的负向**真生效** —— 通用正/负向词直接抄 `07` 的"附：SD 通用正/负面词" |
| 权重只给关键项 | 风格词、光位词、构图词提权；不要每项都加括号 |
| 分辨率 | ⛔ 不要在提示词里写分辨率，用 sampler 设置 |
| 出图比例 | 靠宽高设置，不是靠提示词 |

⚠️ **cfg=1 / 加装 Lightning、Turbo 等加速蒸馏的模型，负向框会失效**（填了不报错，就是不生效）。
这类情况退回自然语言类的正向化写法（见 05）。
